var config = {
    port: 3080,

    //should we console.log everything?
    verbose: true,

    // board area
    ba: 128,

    // countdown time in ms
    countdown_length: 3000,

};

module.exports = config;
